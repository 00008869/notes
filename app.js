const express = require('express')
const app = require()

app.get('/', (req, res) => ){

}

app.set('view engine'. 'pug')

app.listen(8000, err =>{
	if (err) throw err

	console.log("App is running ....")
})